apiVersion: apps/v1
kind: Deployment
metadata:
  name: autoscale-api
  namespace: pre6g-dashboard
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: autoscale-api
  template:
    metadata:
      labels:
        app: autoscale-api
    spec:
      serviceAccountName: autoscale-api
      nodeSelector:
        kubernetes.io/hostname: icclz2
      initContainers:
        - name: install-python-deps
          image: python:3.12-slim
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -lc
            - >
              python -m pip install --no-cache-dir
              -r /workspace/autoscale-source-split/03-shared-api-dashboard/requirements.txt
              -t /python-deps
          volumeMounts:
            - name: workspace
              mountPath: /workspace
              readOnly: true
            - name: python-deps
              mountPath: /python-deps
      containers:
        - name: autoscale-api
          image: python:3.12-slim
          imagePullPolicy: IfNotPresent
          workingDir: /workspace/autoscale-source-split/03-shared-api-dashboard/autoscale_api
          command:
            - /bin/sh
            - -lc
            - >
              if ! command -v ssh >/dev/null 2>&1; then
              apt-get update &&
              apt-get install -y --no-install-recommends openssh-client &&
              rm -rf /var/lib/apt/lists/*;
              fi &&
              python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: PYTHONPATH
              value: /python-deps:/workspace/autoscale-source-split/03-shared-api-dashboard/autoscale_api:/workspace/autoscale-source-split/01-monitoring-layer
          envFrom:
            - configMapRef:
                name: autoscale-api-config
            - secretRef:
                name: autoscale-api-secret
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          volumeMounts:
            - name: workspace
              mountPath: /workspace
              readOnly: true
            - name: python-deps
              mountPath: /python-deps
            - name: kubectl-bin
              mountPath: /usr/local/bin/kubectl
              readOnly: true
            - name: autoscale-runtime
              mountPath: /workspace/autoscale-source-split/03-shared-api-dashboard/autoscale_api/app/runtime
            - name: autoscale-runtime-root
              mountPath: /workspace/autoscale-source-split/03-shared-api-dashboard/autoscale_api/runtime
            - name: yolo-demo-runtime
              mountPath: /workspace/autoscale-source-split/02-experiment-layer/experiments_yolo/yolo_demo/runtime
            - name: ssh-config
              mountPath: /root/.ssh
              readOnly: true
      volumes:
        - name: workspace
          hostPath:
            path: /home/icclz2/Pre6G
            type: Directory
        - name: python-deps
          emptyDir: {}
        - name: kubectl-bin
          hostPath:
            path: /usr/local/bin/kubectl
            type: File
        - name: autoscale-runtime
          hostPath:
            path: /home/icclz2/Pre6G/autoscale-source-split/03-shared-api-dashboard/autoscale_api/app/runtime
            type: DirectoryOrCreate
        - name: autoscale-runtime-root
          hostPath:
            path: /home/icclz2/Pre6G/autoscale-source-split/03-shared-api-dashboard/autoscale_api/runtime
            type: DirectoryOrCreate
        - name: yolo-demo-runtime
          hostPath:
            path: /home/icclz2/Pre6G/autoscale-source-split/02-experiment-layer/experiments_yolo/yolo_demo/runtime
            type: DirectoryOrCreate
        - name: ssh-config
          hostPath:
            path: /home/icclz2/.ssh
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: autoscale-api
  namespace: pre6g-dashboard
spec:
  type: NodePort
  selector:
    app: autoscale-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30081
